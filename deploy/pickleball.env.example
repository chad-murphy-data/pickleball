# Supabase warehouse credentials for the nightly rally-log upsert.
# Copy to deploy/pickleball.env (gitignored) on the droplet and fill in.
# Get these from the Supabase dashboard → Project Settings → API:
#   SUPABASE_URL          = the Project URL   (https://<ref>.supabase.co)
#   SUPABASE_SERVICE_KEY  = the service_role key (secret; bypasses RLS)
# Without this file the harvest still runs; upload_supabase.py just no-ops.
SUPABASE_URL=https://nwgxyytowbluuykbdcfc.supabase.co
SUPABASE_SERVICE_KEY=
