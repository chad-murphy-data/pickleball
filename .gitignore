__pycache__/
*.pyc
.venv/
raw/
*.log
scraper/discovery/
model/*.npz
# clutch null bootstrap: 60 replayed seasons, ~30 MB each, regenerable in
# ~40 min via model/clutch_null.py (the small obs/games npz stay committed)
data/clutch_null.npz
data/clutch_null_model.npz
.netlify/
node_modules/
site/

# social preview renders (regenerable from the HTML)
content/**/*.png

# droplet Supabase creds (service-role key — never commit)
deploy/pickleball.env

# rare-trait raw dump: 30 replicates x 7 arms, regenerable via clutch_team.py
data/clutch_team_raw.npz

# per-game null baseline cache: regenerable via clutch_partner.py (~15 min)
data/clutch_pergame.npz

# vision probe renders (broadcast-derived imagery stays out of the public
# repo; regenerable from any clip via vision/probe_clip_*.py)
data/vision/*.png
# full-skeleton pose streams (vision/pose_extract.py; regenerable from
# the VOD, ~0.2 MB/rally compressed)
data/vision/pose/

# generated audit tool pages — rebuilt locally by their make_*.py; tracking them
# made every local regeneration block git pull
data/vision/state_audit_chicago0725.html
data/vision/ball_audit_r1.html
data/vision/ball_visibility_audit.html
data/vision/court_calibration.html
data/vision/court3d_r1.html
social/out/
